// let today = new Date();

// let day = today.getDay();

// let daylist = ["Sunday","Monday", "Tuesday","Wednesday","Thuesday","Friday","Saturday"];

// console.log(daylist[day]);

// let hours = today.getHours();
// let minutes = today.getMinutes();
// let second = today.getSeconds();

// let prepand = (hours >= 12)? 'Pm':'Am';

// hours = (hours >= 12)? hours-12 : hours;

// console.log("Current Time: " + hours + prepand + " : " + minutes + " : " + second); 


//Write a JavaScript program to find the area of a triangle where three sides are 5, 6, 7.  


// Define the lengths of the three sides of a triangle
// var side1 = 5; 
// var side2 = 6; 
// var side3 = 7; 

// // Calculate the semi-perimeter of the triangle
// var s = (side1 + side2 + side3) / 2;

// // Use Heron's formula to calculate the area of the triangle
// var area = Math.sqrt(s * ((s - side1) * (s - side2) * (s - side3)));

// // Log the calculated area to the console
// console.log(area);


//Write a JavaScript program to rotate the string 'w3resource' in the right direction. This is done by periodically removing one letter from the string end and attaching it to the front.

let str = 'w3resource';

let lIndex = str.length;

new s = '';

for(let i = 0; i<str.length; i++) {
    console.log(str.charAt(i));

    if(true) {
        str = str
    }
}